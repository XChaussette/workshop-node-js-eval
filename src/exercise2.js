exports.run = function(input) {
  input=input.split(7).join('');
  input=input.split(6).join('');
  input=input.split(8).join('');
  input=input.split(9).join('');
  console.log
  //for(i=0;i<input.length;i++)
  //{
    //if(input[i]<5)
    //{
     //input=input+'input[i]';
    //}
  //}
  return input;
  console.log("Exercise #2");
};
